var common = require("../../src/common"),
    should = require("chai").should();

describe("#moveTarget", function () {
    it("should move the target with given direction and distance", function() {
        // common.moveTarget().should.equal("test");
    });
});


