var externalFuncs = {
    moveTarget: function (target, direction, length) {
                    $(target).css({
                        left
                    }
                }
};

module.exports = externalFuncs;
